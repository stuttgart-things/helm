---
releases:
  - name: vault-deployment
    installed: true
    namespace: vault
    chart: bitnami/vault
    version: 1.7.0
    values:
      - "values/vault.values.yaml.gotmpl"

repositories:
  - name: bitnami
    url: registry-1.docker.io/bitnamicharts
    oci: true
